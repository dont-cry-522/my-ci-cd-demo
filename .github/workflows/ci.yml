name: 我的第一个CI/CD流水线

on: [push]

permissions:
  contents: write  # 添加这行，给工作流写入权限

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
    
    - name: 运行“测试”
      run: |
        echo "🚀 开始测试..."
        ls -la  # 列出文件看看
        
    # 使用GitHub官方的Pages部署
    - name: 部署到GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: gh-pages  # 部署到的分支
        folder: .  # 要部署的文件夹
